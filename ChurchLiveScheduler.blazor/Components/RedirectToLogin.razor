@inject NavigationManager Navigation

@code {
    protected override void OnInitialized()
    {
        // forceLoad: true is essential to break out of the Blazor circuit
        // and trigger the server-side OIDC challenge.
        Navigation.NavigateTo("login?returnUrl=" +
            Uri.EscapeDataString(Navigation.Uri), forceLoad: true);
    }
}